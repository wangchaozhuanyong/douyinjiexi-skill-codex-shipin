{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "QA report",
  "type": "object",
  "required": ["status", "quality_level", "scores", "hard_gates", "evidence_runtime_ratio", "blocking_issues", "warnings", "revision_required"],
  "properties": {
    "status": { "enum": ["passed", "failed"] },
    "quality_level": { "enum": ["publishable", "high_quality", "breakout_potential"] },
    "scores": { "type": "object" },
    "hard_gates": { "type": "object" },
    "evidence_runtime_ratio": { "type": "number", "minimum": 0 },
    "blocking_issues": { "type": "array", "items": { "type": "string" } },
    "warnings": { "type": "array", "items": { "type": "string" } },
    "revision_required": { "type": "boolean" }
  }
}
